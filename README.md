# TugasPBO14
Membuat Halaman Login, Register dan Lupa Password yang  Tersimpan di Database Postgresql diNetbeans. 
# JAVA PERSISTENCE API (JPA) 
Java Persistence API (JPA) adalah spesifikasi resmi Java yang digunakan untuk memetakan data dari database relasional ke dalam bentuk objek Java (Object Relational Mapping/ORM). Dengan JPA, pengembang dapat mengelola, menyimpan, dan mengambil data dari database tanpa perlu menulis banyak kode SQL secara langsung. JPA bekerja dengan cara menghubungkan kelas Java yang disebut Entity dengan tabel di dalam database. Setiap objek dari kelas tersebut mewakili satu baris data di tabel. Proses pengelolaan data dilakukan melalui EntityManager, yang bertugas untuk menambah, memperbarui, mencari, dan menghapus data. Selain itu, JPA juga menggunakan JPQL (Java Persistence Query Language), yaitu bahasa query yang mirip dengan SQL tetapi bekerja menggunakan nama kelas dan atribut, bukan nama tabel dan kolom. Konfigurasi koneksi dan pengaturan entitas JPA biasanya disimpan dalam file persistence.xml yang mendefinisikan unit persistensi. JPA hanya merupakan spesifikasi, sehingga untuk menggunakannya dibutuhkan implementasi seperti Hibernate, EclipseLink, atau OpenJPA. Dengan JPA, pengembangan aplikasi menjadi lebih efisien karena data dapat diolah langsung melalui objek Java tanpa perlu berinteraksi langsung dengan perintah SQL, serta mendukung berbagai sistem database dengan sedikit perubahan kode. 
# HALAMAN LOGIN 
Halaman login merupakan halaman awal yang digunakan pengguna untuk mengakses sistem dengan memasukkan username dan password. Pada aplikasi ini, proses autentikasi dilakukan dengan menghubungkan halaman login ke database menggunakan JPA (Java Persistence API). JPA berfungsi sebagai penghubung antara program Java dan database, sehingga data pengguna dapat diakses atau diverifikasi secara langsung melalui entitas (entity class). Pada implementasinya di NetBeans, dibuat kelas entitas bernama Login yang berisi atribut seperti id, username, dan password. Kelas ini diberi anotasi @Entity agar dikenali sebagai representasi tabel pada database. JPA akan secara otomatis memetakan kelas tersebut ke tabel yang ada di database, misalnya tabel login. Saat pengguna menekan tombol Login, sistem akan menjalankan query menggunakan 
EntityManager untuk memeriksa apakah kombinasi username dan password yang dimasukkan sesuai dengan data yang tersimpan di database. Jika data valid, pengguna akan diarahkan ke halaman utama aplikasi. Namun, jika data tidak ditemukan atau salah, maka sistem akan menampilkan pesan kesalahan. Dengan pendekatan ini, proses login menjadi lebih aman dan efisien karena seluruh interaksi data dikelola oleh JPA tanpa perlu menulis query SQL secara manual. Selain itu, penggunaan JPA memudahkan pengembangan aplikasi karena mendukung konsep Object Relational Mapping (ORM), yang memungkinkan objek Java langsung terhubung dengan tabel database.
# MENAMBAHKAN BUTTON DOWNLOAD PADA GUI 
Pada fitur Download CSV, aplikasi mengambil seluruh data dari tabel melalui JPA. Data tersebut kemudian diubah menjadi format CSV, di mana setiap record dituliskan dalam satu baris. Aplikasi menyediakan JFileChooser agar pengguna bisa memilih lokasi penyimpanan file. Dengan adanya fitur ini, pengguna dapat membuat backup data, memindahkan data, atau mengolahnya di aplikasi lain seperti Excel. Sebaliknya, fitur Upload CSV memungkinkan pengguna membaca file CSV dari komputer dan memasukkan setiap baris data kembali ke database. Program menggunakan BufferedReader untuk membaca baris demi baris, kemudian memetakan setiap kolom ke atribut entity. EntityManager digunakan untuk melakukan operasi persist sehingga data langsung masuk ke tabel. Sistem juga dapat melakukan validasi seperti melewati header CSV, mengecek kolom kosong, dan mencegah error ketika baris tidak sesuai format.
# Langkah - langkah 
1. Menambahkan jButton Pada panel gui
2. Mengganti textnya menjadi Download button
3. Pemrograman Tombol Download CSVPada event tombol, gunakan EntityManager untuk mengambil semua data dari tabel.
4. Konversikan data menjadi baris CSV dan simpan menggunakan JFileChooser. Setiap entitas disusun menjadi satu baris dengan pemisah koma atau sesuai dengan versi excel masing masing file
5. Pemrograman Tombol Upload CSV Gunakan JFileChooser untuk memilih file CSV.
6. Gunakan BufferedReader untuk membaca baris CSV. Lewati header, lalu pecah data dengan split(","). Buat entity baru untuk setiap baris dan simpan menggunakan em.persist().
7. Pengujian Fitur Download & Upload CSV isi data terlebih dahulu lalu klik button gui lalu save kedalam dokumen komputer anda.
8. Lalu hapus semua data  yang sudah di input tadi.
9. Klik Button Upload lalu memilih file csv yang sudah disimpan/download tadi, lalu klik oke data akan ter upload kembali ke tabel sesuai dengan data yang di input sebelumnya
